# Configura�ao do banco de dados (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/SENAI
spring.datasource.username=root
spring.datasource.password=bacca452

# Exibe os comandos SQL no terminal
spring.jpa.show-sql=true

# Formata os comandos SQL no terminal
spring.jpa.properties.hibernate.format_sql=true

# N�o exibe a stacktrace durante uma requisicao HTTP
server.error.include-stacktrace=never

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update

# Permite ao hibernate gerar codigo SQL otimizado para um banco de dados especifico
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect